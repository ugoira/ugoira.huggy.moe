>div
    #class=i_{{id}}
    #id=s{{sid}}
    >a
        #target={{id}}
        #download
        #href={{url}}
        >video
            #src={{url}}
            #preload=auto
            #autoplay=autoplay
            #loop=loop
            #muted=muted