>div
    >a
        #download={{id}}
        #href={{url}}
        >video.max-w-full.mx-auto
            #src={{url}}
            #preload=auto
            #autoplay
            #loop
            #muted